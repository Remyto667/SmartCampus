{% extends 'basewithnavbar.html.twig' %}

{% block body %}

    <style>
        /************** CSS **************/
        .img{
            /*background-attachment: fixed;
            top: 0;
            left: 0;*/
            width: auto;
            height: 970px;
        }
    </style>

        {% if app.user.username == 'admin'%}
            <img class="img" src="../img/guide_admin.png">
        {% elseif app.user.username == 'technicien'%}
            <img class="img" src="../img/guide_technicien.png">
        {% else %}

        {% endif %}
{% endblock %}

