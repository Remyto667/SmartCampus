{% extends 'basewithnavbar.html.twig' %}

{% block body %}

    <style>
        /************** CSS **************/
        img{
            /*background-attachment: fixed;*/
            background-size: cover;
        }
    </style>

        {% if app.user.username == 'admin'%}
            <img src="../img/guide_admin.png">
        {% elseif app.user.username == 'technicien'%}
            <img src="../img/guide_technicien.png">
        {% else %}

        {% endif %}
{% endblock %}

