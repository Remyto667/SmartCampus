{% extends 'basewithnavbar.html.twig' %}

{% block title %}Selection d'une salle{% endblock %}

{% block body %}
    <style>
        /************** CSS **************/
        /**** General characteristics ****/
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

        body{
            text-align: center;
            margin-left: 180px;
        }
        h2 {
            margin: 30px;
            font-size: 20px;
        }

        /**** Buttons and colors ****/
        .btn
        {
            align-items: center;
        }
        .green {
            color: #fff;
            background-color: #75A307;
            border-color: #75A307;
            text-decoration: none;
            display: inline-block;
            font-weight: 400;
            text-align: center;
            white-space: nowrap;
            vertical-align: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
            padding: 0.375rem 0.75rem;
            font-size: 1rem;
            line-height: 1.5;
            border-radius: 0.25rem;
            transition: color .15s ease-in-out,background-color .15s ease-in-out,border-color .15s ease-in-out,box-shadow .15s ease-in-out;
        }
        .red {
            color: #fff;
            background-color: #B51b1b;
            border: #B51b1b;
            text-decoration: none;
            display: inline-block;
            font-weight: 400;
            text-align: center;
            white-space: nowrap;
            vertical-align: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
            padding: 0.375rem 0.75rem;
            font-size: 1rem;
            line-height: 1.5;
            border-radius: 0.25rem;
            transition: color .15s ease-in-out,background-color .15s ease-in-out,border-color .15s ease-in-out,box-shadow .15s ease-in-out
        }
        .red:hover
        {
            background: #da0e0e;
        }

        a:hover {
            color : #FFFFFF;
        }

        /**** Organization of the page ****/
        .content {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            column-gap: 5000px;
            row-gap: 1px;
        }
        .line {
            display : flex;
            justify-content: space-around;
            column-gap: 50px;
        }

        /**** Outlines for each room ****/
        .boxItem {
            width : 150px;
            height: 100px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;
            column-gap: 500px;
            row-gap: 10px;

            padding: 10px;
            border: 1px solid grey;
            border-radius: 6px;
            box-shadow: 2px 2px 5px grey;
        }

        .boxItem:hover {
            box-shadow: 4px 4px 7px grey;
        }

        /**** Legends ****/
        .legend
        {
            display: flex;
            justify-content: space-around;
            margin-top: 50px;
        }
        .legendGreen
        {
            border-bottom: 3px solid #75A307;
            color: #75A307;

        }
        .legendRed
        {
            border-bottom: 3px solid red;
            color: #B51b1b;
        }
        .text{
            text-align: center;
        }

    </style>

    <div class="example-wrapper">
        <h1>Choisir une salle pour voir le détail des alertes</h1>
        <h2>Vous pouvez voir le nombre d'alertes détectées au cours du dernier mois</h2>
        <br>
        <div class="content">
            {% for floor in allFloor | reverse %}
                <!-- With an exception for floor 0 where we display : ground floor -->
                {% if floor=="0" %}
                    <p style="margin-top: 20px;"> Rez-de-chaussée : </p>
                {% else %}
                    <p style="margin-top: 20px;"> Etage {{ floor }} : </p>
                {% endif %}
                <div class="line">
                    {% for room in allRoom %}
                        <!-- All the rooms corresponding to the floor are displayed, without taking into account the stock -->
                        {% if (room.isIsStock() != 1) and (room.floor == floor) %}
                            <div class="boxItem" onclick="window.location='/admin/alerte_stat/{{ room.id }}';">
                                {% if nbAlert[room.id]["T"]["More"] + nbAlert[room.id]["T"]["Less"] + nbAlert[room.id]["H"]["More"] + nbAlert[room.id]["H"]["Less"] + nbAlert[room.id]["C"]["More"]+ nbAlert[room.id]["C"]["Less"]  > 1000 %}
                                    <a class="btn red btn-danger" href="/admin/alerte_stat/{{ room.id }}">{{ room.name }}</a>
                                {% else %}
                                    <a class="btn btn-primary green" href="/admin/alerte_stat/{{ room.id }}">{{ room.name }}</a>
                                {% endif %}
                                <p class="text">{{  nbAlert[room.id]["T"]["More"] + nbAlert[room.id]["T"]["Less"] + nbAlert[room.id]["H"]["More"] + nbAlert[room.id]["H"]["Less"] + nbAlert[room.id]["C"]["More"]+ nbAlert[room.id]["C"]["Less"] }} alertes</p>
                            </div>
                        {% endif %}
                    {% endfor %}
                </div>
                <br>
                <br>
            {% endfor %}
        </div>

        <div class="legend">
            <p class="legendGreen">Salle sans problèmes</p>
            <p class="legendRed">Salle avec Alerte(s)</p>
        </div>

    </div>
{% endblock %}