{% extends 'logo.html.twig' %}

{% block title %}Données d'une salle{% endblock %}

{% block body %}
    <style>
            /*.example-wrapper { text-align: center; margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }*/
            body{
                background-color: #f7f9fa;
            }

        .body{
            height: 100%;
            font-family: arial;
            text-align: center;
        }

            /************************/

            .title
            {
                position:absolute;
                text-align:center;
                width:100%;
                font-size: 40px;
            }

            .titleEffect {
                position: relative;
                color: black;
                text-decoration: none;
            }

            .titleEffect:after {
                content: '';
                display: block;
                position: absolute;
                bottom: 0px;
                width: 0%;
                left: 0px;
                height: 3px;
                transition: 0.25s cubic-bezier(0.72,-0.01, 0.19, 0.92);
                background: #014898;
            }

            .titleEffect:hover:after {
                width: 100%;
            }

            /************************/

        /************************* TITRE **************************/
        .salle {
            margin-top: 100px;
            margin-left: 46%;
            transform: translateY(-50%);
            width: 200px;
        }
        .shape {
            stroke-dasharray: 140 540;
            stroke-dashoffset: -474;
            stroke-width: 8px;
            fill: transparent;
            stroke: #014898;
            transition: stroke-width 1s, stroke-dashoffset 1s, stroke-dasharray 1s;
        }
        .text {
            display: block;
            line-height: 32px;
            /*text-decoration:underline;*/
            letter-spacing: 7px;
            top: -47px; /* Monter le text dans le rectangle plus haut */
            position: relative;
            text-align: center;
            font-size: 50px;
            justify-content: center;
        }
        .salle:hover .shape {
            stroke-width: 4px; /* Epaisseur du contour plus fine que avant */
            stroke-dashoffset: 0;
            stroke-dasharray: 800;
        }

        /************************* GRILLE **************************/
        .data-container{
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around; /* Espacé sans être collé aux bords */
            /*margin: 100px;*/
        }

        .box {
            border: 0.5px solid grey;
            border-radius: 8px;
            box-shadow: 2px 2px 5px grey;
            width: 250px;
        }

        .box:hover{
            /*color: #014898;
            font-size: 25px;*/
            box-shadow: 5px 5px 5px grey;
            text-shadow: 2px 2px 5px grey ;
        }

        .titreDonnee{
            font-size: 20px;
            /*padding: 10px;*/
        }

        .donnee{
            padding: 50px;
            font-size: 35px;
            border: 0.5px solid grey;
            border-radius: 8px;
            box-shadow: 2px 2px 5px grey;
        }

            .boxDesc{
                border: 2px solid grey;
                border-radius: 32px;
                box-shadow: 2px 2px 5px grey;
                padding-bottom: 30px;
                padding-left: 20px;
                padding-right: 30px;
            }

        /****** BOUTONS ALERTE ******/

        .boutonAlerte{
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            margin-top: 50px;
        }

        .boxBouton {
            /*border: 0.5px solid grey;
            border-radius: 8px;*/
            width: 250px;
        }

        /*************************/
        .description{
            display: flex;
            flex-wrap: wrap;
            justify-content: space-evenly;
            margin-top: 100px;
        }

        /********* EFFET *********/

        @keyframes box{
            0%,
            100%{
                transform: scale(1);
            }
            50%{
                transform: scale(1.3);
            }
        }


            {% if temp > room.getType().getTempMax() or temp < room.getType().getTempMin() %}
            .Temp {color: white; border: 1px solid black; background-color: #B51b1b;}
            .box:nth-child(1){
                transform: scale(1);
                animation: box 1s linear infinite;
            }
            {% endif %}
            {% if hum > room.getType().getHumMax() or hum < room.getType().getHumMin() %}
            .Hum {color: white; border: 1px solid black; background-color: #B51b1b;}
            .box:nth-child(2){
                transform: scale(1);
                animation: box 1s linear infinite;
            }
            {% endif %}
            {% if co2 < room.getType().getCo2Min()  or co2 >= room.getType().getCo2Max() %}
            .C02 {color: white; border: 1px solid black; background-color: #B51b1b;}
            .box:nth-child(3){
                transform: scale(1);
                animation: box 1s linear infinite;
            }
            {% endif %}

        .btn{margin-top: 15px; background-color: #B51b1b; color: white; max-width: 250px;}
        .btn-blue{background-color: #00489a; color: white;}

    </style>

    <div class="body">

        <div class="text_center">
            <br>
            <div class="title">
                <a class="titleEffect">{{ room.getName() }}</a>
            </div>
            <br>
            <br><br><br><br><br>
            <br>

            <!--<div class="salle">
                <svg height="60" width="200" xmlns="http://www.w3.org/2000/svg">
                  <rect class="shape" height="60" width="200" />
                    <div class="text">{/{ room.getName() }}</div>
                </svg>
            </div>-->

            <div class="container">
                <div class="data-container">

                    <div class="bg-white box">
                        <div class="titreDonnee">Température</div>
                        {% if temp != "NULL"%}

                            <div class="Temp donnee">{{ temp }} °C</div>
                            Date de relevé : {{ dateT }}
                        {% else %}
                            <div class="bg-white donnee">Pas de<br>donnée</div>
                        {% endif %}
                    </div>

                    <div class="bg-white box">
                        <div class="titreDonnee">Humidité</div>
                        {% if hum != "NULL"%}

                            <div class="Hum donnee">{{ hum }} %</div>
                            Date de relevé : {{ dateH }}
                        {% else %}
                            <div class="bg-white donnee">Pas de<br>donnée</div>
                        {% endif %}
                    </div>
                    <div class="bg-white box">
                        <div class="titreDonnee">CO2</div>
                        {% if co2 != "NULL"%}

                            <div class="C02 donnee">{{ co2 }} PPM</div>
                            Date de relevé : {{ dateC }}
                        {% else %}
                            <div class="bg-white donnee">Pas de<br>donnée</div>
                        {% endif %}
                    </div>
                </div>


            </div>
        </div>
        <div class="description">
            <div class="textSensor boxDesc bg-white">
                <br><br>
                <ul>Des conseils vous seront donnés si :
                    <li>- {{ room.getType().getTempMin() }} °C / + {{ room.getType().getTempMax() }} °C </li>
                    <li>-{{ room.getType().getHumMin() }} % / + {{ room.getType().getHumMax() }}% d'humidité </li>
                    <li>- {{ room.getType().getCo2Min() }} PPM / +{{ room.getType().getCo2Max() }}PPM </li>
                </ul>
            </div>
            {% if temp > room.getType().getTempMax() or temp < room.getType().getTempMin() or hum > room.getType().getHumMax() or hum < room.getType().getHumMin() or co2 < room.getType().getCo2Min()  or co2 >= room.getType().getCo2Max() %}
                <div class = "description boxDesc bg-white">
                    <div class="descriptionRoom">
                        <br><br>

                        <p class ="font-weight-bold h5" style="border-bottom: 2px solid #014898; color : #B51b1b;">Conseil</p>
                        <p>{{ conseil.conseil }}</p>
                        <p>{{ conseil.getNoData() }}</p>
                        {% if conseil.getNoData() %}
                            <p>Un des capteurs est peut-être hors service</p>
                        {% endif %}
                        {% if conseil.getCo2AlerteInf() %}
                            <p>Il semblerait que le capteur est défaillant, veuillez alerter le service technique ou un supérieur</p>
                        {% endif %}
                    </div>
                </div>
            {% endif %}
        </div>
        <br>
        <a class="btn btn-blue btn-primary" href="/salle/selection" role="button">Retour</a>
    </div>
{% endblock %}
